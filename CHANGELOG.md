# 更新日志 / Changelog

> **English version: [CHANGELOG_EN.md](CHANGELOG_EN.md)**

本文件记录 **Aeronautics: Tweaks & Toolkit**（mod id 仍为 `sablestopnow`）的版本变更。
格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [1.1.1] — 2026-09

物理手杖换了一套**以滚轮为中心**的新控制逻辑与 HUD；缩放从「只是看着变大」变成**真的改变碰撞体与质量**，并且**实体碰撞也遵守缩放**。

### 新增 Added

- **新控制逻辑**（`new_control_scheme`，默认开，覆盖原键位）：**Ctrl** 切多选、**滚轮**切功能、**左键**应用当前功能；旧的 Z/V/O/K/R/C/X 只作为「功能项」存在。三种模式（普通 / 多选 / 整组拖拽）各有自己的功能清单，每个模式记住上次选中的功能。
  - **持续型功能会接管鼠标与滚轮**：缩放＝左键开始 → 滚轮调大小 → 左键确认；区域选择＝左键开始 → 右键定角点 → 滚轮调距离 → 左键取消；按住型（视角锁定 / 整组归中）＝按住左键生效。
  - **Ctrl 是「点击」语义**：从按下到松开之间**没有任何其它操作**才算点击；期间点过鼠标或滚过滚轮就只算组合键（Ctrl+滚轮在普通/多选模式交还原版切物品栏）；**整组拖拽模式下 Ctrl 不切多选**。
  - **未启用状态**：刚把手杖切到手上时**不进入任何模式**，并且**忽略除「左键启用」与「滚轮切物品栏」之外的所有手杖操作**（右键/中键、航空学原生的右键拖拽、Ctrl 切多选全部不生效），HUD 提示「左键启用」；按一次左键即启用，切走手杖自动复位。
  - 视角锁定（原长按 C）在新逻辑下没有对应按键，因此**新增为功能项**（普通模式，按住左键），功能不变。
  - **普通模式与整组拖拽模式的第一个功能固定为「锁定切换」**（对应原版左键锁定），启用后光标默认就停在它上面。
- **单个结构也能进入拖拽模式**：右键一个不在队列里的结构会为它建一个**临时单成员组**并进入整组拖拽（临时组与主多选队列**并存、互不干扰**，退出拖拽即清除，且不画蓝色描边）。
- **功能作用于「正在看的结构（组）」**：锁定切换 / 无碰撞 / 快照 / 缩放 / 所有权 先看准星 —— 看着的是组成员则作用于**整组**，看着的是组外结构则只作用于**它一个**，没看着任何结构才回退到当前组（以前是非空队列直接抢先）。
- **模组图标**：`src/main/resources/icon.png` + `mods.toml` 的 `logoFile`，NeoForge 模组列表会显示手杖+齿轮图标。
- **新 HUD**：左上角「模式斜切标签 + 该模式全部功能 + 会平滑滑动的蓝色选框 + 带左侧三角凸起的描述面板（自动换行）+ 换行提示」；模式切换时整块先向左消失再从左侧进入，描述从右淡出后从左到右出现，应用功能时沿选框周长跑一条连续光条（Z/X 持续旋转到动作结束）。配色沿用自定义设置界面那套蓝，**文案跟随游戏当前语言**。
- **右上角瞄准结构信息面板**（`show_body_info`，默认开）：拿着手杖时显示准星所指结构的所有者 / 速度 / 质量 / 缩放 / 碰撞 / 快照，从右侧滑入滑出。速度与质量只有服务端有，走一对新的请求/应答包按需查询（换目标立即查、同目标每 10 tick 刷新）。
- **准星右侧实时显示穿透层数。**
- **缩放现在真正重建 rapier 碰撞体**（移植自 `sable-scale`）：重采样体素格，让原生「未缩放位姿」变换正好把碰撞盒落在缩放后的外壳上；回到 ×1 时移除重采样并恢复原生上传。**质量随之变化**（`m'=k·m`，惯量按二阶矩变换）。
- **实体碰撞遵守缩放**：修复玩家「按缩放前大小被推」的问题。
- **缩放值改走 Sable 自己的通道**：`SableBufferUtils`（位姿过网络的唯一漏斗）+ `SableNBTUtils`（落盘），删掉本模组自建的同步包与 SavedData。

### 变更 Changed

- `scaled_no_player_collision` 默认改为 **false**：碰撞体会随缩放重建，缩放后不再对玩家幽灵化（想要「穿透」效果时再手动打开）。
- 功能列表不再按距离虚化（全部同一亮度）。

### 修复 Fixed

- **Ctrl 无法进入多选**：同一个 tick 内对同一个 `KeyMapping` 调了两次上升沿判定，第一次就把边沿消费掉了。
- **缩放后与玩家没有碰撞**：旧配置里 `scaled_no_player_collision=true` 是已存在的键，NeoForge 只在键缺失/非法时纠正，所以改默认值对已有配置无效。
- **模型与碰撞边框缩放不一致**（两个独立根因）：① Sable 自己的方块描边把缩放**乘错了顺序**（`T(pos−cam)·R·T(plotCam−rotPoint)·S`，正确是 `…·R·S·T(plotCam−rotPoint)`，误差是纯平移 `R·(I−S)·(plotCam−rotPoint)`）→ 用 priority 1000 的 `@WrapOperation` 在它内层补正；② **单方块渲染路径压根不读 scale**（`renderSingleBlock` 只有 translate→rotate）→ 补 Mixin。轮廓渲染器也改用逐轴缩放。
- HUD：模式标签描边不连续（上边没画到斜切顶端）、光条稀疏采样有断点、滚轮方向与列表相反、选框只在整行之间跳变、描述「出现」方向反了 —— 全部修正。
- **清空队列 / Shift+右键「清不掉」选择**：`releaseAllClaims()` 在本地标记已复位时会提前 return（服务端占用未必已释放），且「清空队列」只清了主队列、没清临时拖拽组；现在清空时**无条件通知服务端**并连带清掉临时组。

### 已知限制 Known limitations

- **Flywheel 实例化方块**（Create 的轴 / 齿轮 / 传送带）不跟随缩放。
- **scale≠1 时的拾取 / 破坏框定位**可能偏（Sable 的 `BlockGetter.clip` 用的是未缩放的 broadphase）。
- 关节 / 马达的锚点不随缩放重设（需「先缩放再组装」）；原版的 `TerrainClearance`（缩放后重新贴地）未移植。
- 旧的 `sablestopnow_scale.dat` 不再读取，老存档里已缩放的体会回到 ×1（重新缩放即可）。
- **协议被扩展：服务端与客户端都必须装本模组。**

## [1.1.0] — 2026-09

物理手杖增强从「能用」推到「完整工具链」：多人并存、所有权、区域选择重做、快照、缩放、幽灵化，以及一个完整的游戏内设置界面。

### 新增 Added

- **模组更名**：显示名改为 **Aeronautics: Tweaks & Toolkit**（mod id / 包名保持 `sablestopnow` 不变，存档兼容）。
- **多人独立选择**：每个玩家有独立的选择队列；同一物理结构同一时刻只能被一个玩家选中，已被他人选中或拥有的体会被拒绝并在聊天栏提示是谁；描边颜色按玩家从 12 色调色板分配，不同玩家不同色；他人的选择只在同维度 64 格内渲染。
- **所有权（O）**：整队列设置/取消所有权，落盘保存（`SavedData`）跨重启保留；准星指向某结构时 HUD 显示所有者；OP 后门命令 `/sablesn owner list | clear <玩家> | clear all`。
- **穿透拾取随时可用**：左 Alt + 滚轮调整穿透层数，不再要求先进入多选模式。
- **悬停轮廓**：任意时刻都能看到「可能被选中」的物理结构轮廓；若该体属于当前选择队列，则**整组轮廓加粗**。
- **区域选择（Z）重做**：按 Z 进入；**滚轮**调选择点到玩家的距离、**视线**定方向；第一个角点前显示 1 格大小的预览框；**右键**确认角点，两点框出长方体；在选第二个点时**实时以黄绿色预描边「将会被选中」的结构**；再按 Z 取消。区域选择的滚轮不会切换快捷栏。
- **视角锁定（C）**：手持手杖且不在多选/整组控制时，长按 C 每 tick 重新选取「与视线夹角最小」的物理结构并把视角锁定其上，松开解锁；整组控制中 C 仍是「归中」。
- **快照 / 回退（K / R）**：K 为整个选中队列创建/取消快照（成员上方显示浅蓝相机图标），R 把快照整体回退——方块、位置、朝向、速度、名称全部回到创建快照那一刻；快照按玩家独立。
- **缩放（X + 滚轮）**：以整组**公共质心**为基准等比缩放，成员相对位置与朝向一起缩放，形状不散架；倍率范围与灵敏度可配置；HUD 显示当前倍率与准星所指结构的倍率；首次按 X 会把混合倍率归一化到 1.0 再开始缩放。
- **幽灵化（不与玩家碰撞）**：新增 `dragged_no_player_collision`（正在被拖拽的体不再与**拖拽它的玩家**碰撞）与 `scaled_no_player_collision`（缩放 ≠ 1 的体不再与**任何玩家**碰撞），默认开启。
- **游戏内设置界面**：`Ctrl+O`（或模组列表的「配置」按钮）打开，按 `newConfigGUI/` 的设计实现大类选择页与详情页（进场/离场动画、图标缩放与旋转、折线轨迹排布），全部文案为「中文 | English」，改动即时写回 `config/sablestopnow-common.toml`。
- **按键全部迁移到 Minecraft `KeyMapping`**，可在「选项 → 按键控制 → Aeronautics: Tweaks & Toolkit」里随时改键，无需重启。

### 变更 Changed

- **框选与「仅轮廓」描边风格**：由 GL 线条改为 Create 蓝图 / 强力胶风格的**细长长方体盒**，粗细可通过 `staff_outline_thickness` / `outline_thickness` 调整，并加入平滑动画；其余描边模式视觉保持不变。
- 超速自动锁定的豁免范围扩大：正在被拖拽、正在被缩放或已被缩放过的物理体都不会被误锁。
- **构建产物改名**：jar 文件名前缀由 `sablestopnow` 改为 `aeronautics-tweaks-toolkit`（即 `aeronautics-tweaks-toolkit-1.1.0.jar`）。**mod id 与包名仍是 `sablestopnow`**，只是文件名跟随显示名。
- 手杖相关界面文案全部双语化，HUD 改为「只显示当前正在做的事」。

### 修复 Fixed

- **「仅轮廓」模式向内多描一圈**：旧判定要求一条棱两侧的邻居都存在且都暴露才跳过，导致平坦表面最外圈方块的内部接缝被描出来；现改为只检查跨过该棱的那唯一一个邻居，并对同一方块内的 12 条棱做位掩码去重。
- **渲染崩溃 `IllegalStateException: Not building!`**：1.21.1 的 `MultiBufferSource.BufferSource` 中，不在 `fixedBuffers` 里的自建 `RenderType` 全部共用同一个 `sharedBuffer`，同时持有两种自建类型的 `VertexConsumer` 会提前 `endBatch`；现固定为「先画完所有盒式边框并 `endBatch`，再画线条图标」的两段式绘制。
- **区域选择预览框不可见**：世界坐标顶点缺少 `poseStack.translate(-cameraPos)`。
- **区域选择滚轮误切快捷栏**。
- **切换类按键被 GLFW 重复触发**：原版在按键重复（REPEAT）时也会调用 `KeyMapping.click()`，现统一使用自实现的上升沿判定。
- **多选键（Ctrl）与原版疾跑冲突**：进入多选时每 tick 显式压掉疾跑键状态。
- **设置界面相关**：`rowAt()` 返回可空 `Integer` 直接拆箱导致渲染 NPE；`Ctrl+O` 打开的界面无法用 ESC 关闭；从大类详情页返回时闪烁且不重新加载动画。
- **网络协议错误断线**：`StreamCodec.unit(...)` 只编码创建时捕获的那个实例，导致清空选择 / 回退快照 / 结束缩放三个包在发送时报「Can't encode」并把连接踢回多人菜单；改用 `StreamCodec.of(...)` 手工编解码。
- **缩放不生效 / 服务端来回抹掉**：Sable 的 `SableBufferUtils` 序列化 `Pose3d` 时**不含 `scale`**，`SubLevelPhysicsSystem.readPose` 也只读位置与朝向，且两条区块渲染路径都只做平移+旋转；现由模组自行同步缩放数据，并用 Mixin 把渲染矩阵改写为 `T((1−s)·(pos−cam)) · R · S`。
- **缩放后弹回**：自家超速自动锁定把被马达瞬移的成员锁死了；现对缩放会话成员豁免速度扫描，并在会话开始/结束与中途正确接管锁定状态。

### 已知限制 Known limitations

- 缩放属于**视觉 + 玩家碰撞**层面：Rapier 的碰撞体尺寸与质量不随缩放改变，缩放体之间的物理仍按原尺寸结算；Sable 的序列化会丢掉 `scale`，所以由本模组自行持久化。
- 幽灵化只关闭碰撞，Sable 的搭车（tracking）逻辑仍在：站上去仍可能被带走。
- 「无碰撞」标记不影响地形，Sable 没有 per-body 的「对世界」过滤开关。
- 快照不落盘（服务器重启/退世界即失效），且回退是「移除 + 重建」，会有一瞬间重生；挂在结构上的其它模组关联不保证恢复。
- 整组控制是逐成员马达驱动，超大/高速结构可能有轻微弹动（刚度/阻尼为实测经验值）。

## [1.0.4] — 2026-08

### 新增
- `/sablesn tick <步数>`：物理暂停时步进指定数量的物理 tick，结束后自动恢复暂停。
- 超速自动锁定：物理体速度超过 `speed_limit_threshold` 时自动锁定，并在全服聊天给出**可点击填充传送指令**的提示。

### 修复
- 超速锁定播报刷屏：加入每物理体 60 tick 冷却，且只在确认锁定成功后播报。
- 反射取航空学拖拽会话时可能误取 `locks` 表，导致把普通拖拽体当成「未被拖拽」而误锁。

## [1.0.3] — 2026-07

### 新增
- 无碰撞标记改为对**整个选择队列**生效。
- 常驻 HUD：显示多选状态、穿透层数、已选数量等。
- 实验性 `ghost_real`：真正消除与其它 Sable 物理体之间的碰撞（地形/玩家除外）。

## [1.0.2] — 2026-07

### 新增
- **物理手杖增强**首版：多选（Ctrl）、框选（Z）、穿透选择（Alt+滚轮）、智能锁定切换、整组控制（移动/滚轮调距/长按 C 归中/TAB 旋转）。
- 统一青色「仅描轮廓」选中/悬停线框与圆环图标。
- 双语操作提示。

## [1.0.1] — 2026-06

### 变更
- 物理表面描边 / 坐标轴渲染等显示项整理进配置项，并关闭部分冗余日志。

## [1.0.0] — 2026-06

### 新增
- 首个版本：力阈値过滤（`threshold` + `excluded_groups`）、过滤时自动暂停与全服广播、分页力记录 `/sablesn forces [filtered] [page]`（目标体 ID 可点击填充 `/tp`）、热力图切分前的 `/sablesn confirm` / `/sablesn deny` 确认、新物理体自动锁定、禁用放置碰撞检测等。
